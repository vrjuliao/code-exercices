#include <bits/stdc++.h>

using namespace std;

long size_of_axis(long l, long r) {return r-l;}

int main () {
  int rounds, size, aux;
  cin >> rounds;
  long l,r, al, ar;
  long axis_length;

  while(rounds--){
    cin >> size;
    l = 0;
    al = 0;
    if(--size > 0){
      cin >> r;
      ar = r;
    }
    while(size--){
      cin >> aux;
      // qual deixa mais espaco
      if(size_of_axis(ar-aux, al) - size_of_axis(l, r)
        size_of_axis(ar-aux, al) - size_of_axis(l, r))
      
      if(size_of_axis(ar-aux ) < size_of_axis())

    }

  }


  return 0;
}